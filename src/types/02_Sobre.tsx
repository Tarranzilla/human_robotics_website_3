export default interface SobreType {
    domId: string;

    id: number;
    name: string;
    type: string;
    description: string;
    content: string;
    imgSrc: Array<string>;
    videoSrc: Array<string>;

    furtherInfo: string;
}
